pre-commit:
  commands:
    biome-fix:
      run: cd frontend && bun run check:write
      stage_fixed: true

pre-push:
  commands:
    biome-ci:
      run: cd frontend && bun run ci
    type-check:
      run: cd frontend && bun run type-check
