pre-commit:
  parallel: true
  commands:
    biome:
      glob: "frontend/src/**/*.{ts,tsx,json}"
      run: cd frontend && bun run check --write {staged_files}
      stage_fixed: true

pre-push:
  commands:
    biome:
      run: cd frontend && bun run check
